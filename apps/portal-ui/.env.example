# Portal UI Environment Variables example

# 1. Login & Auth (Used in useLogin.js)
# Default: /api/auth
VITE_GATEWAY=/api/auth

# 2. Homework & User Profile (Used in RepoViewer.jsx, ListTree.js, useProfile.js)
# Warning: In standard Vite, this must exist as VITE_GATEWAY_SERVICE or be allowed in config to be visible.
# Checks import.meta.env.GATEWAY_SERVICE
# Default: /api/homework (or /api/user context sensitive)
GATEWAY_SERVICE=/api

# 3. Core API Fetcher (Used in apiFetch.js)
# Warning: Checks import.meta.env.API_GATEWAY_SERVICE
# Default: http://localhost:3003 (Direct to Class API?)
API_GATEWAY_SERVICE=http://localhost:3004



# Note: Ideally, these should all be unified to a single VITE_API_URL or similar.
# Currently the code expects these different names.